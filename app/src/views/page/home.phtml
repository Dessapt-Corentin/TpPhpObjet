<h1>Bienvenue sur Easyloc</h1>

<?php
// On veux cacher les bouttons une fois connecté
if (isset($_SESSION['user'])) {
    $firstname = $_SESSION['user']->getFirstname();
} else {
    $firstname = ''; // Valeur par défaut si la clé n'est pas présente
}
if ($firstname == '') {
?>

    <div>
        <button><a href="/users/add">Créer un compte</a></button>
        <button><a href="/users/login">Se connecter</a></button>
    </div>

<?php
} else {
?>
    <nav>
        <div class="navbar">
            <!-- <li><a href="/profil">Mon profil</a></li> -->
            <button><a href="/users/listaccomodation/<?php echo $_SESSION['user']->getId() ?>">Voir mes annonces</a></button>
            <button><a href="/users/list-rental/<?php echo $_SESSION['user']->getId() ?>">Mes Réservations</a></button>
            <button><a href="/users/addaccommodation">Ajouter une location</a></button>
            <button><a href="/users/list-who-reserve">Voir les réservations</a></button>
            <button><a href="/users/logout">Se déconnecter</a></button>
        </div>
    </nav>
<?php
}
?>
<h2> Locations </h2>
<?php
if ($firstname != ''): ?>
    <p>Bonjour <?php echo htmlspecialchars($firstname); ?></p>
<?php endif; ?>
<div class="card" style="width: 18rem;">
    <?php
    if (!empty($accommodations)) : ?>
        <div class="accommodations-list">
            <?php foreach ($accommodations as $accommodation): ?>
                <div class="accommodation-item">
                    <img src="/image/<?php echo htmlspecialchars($accommodation->getImage()); ?>" alt="Image du logement">
                    <ul>
                        <li>Pays : <?php echo $accommodation->getAdress()->getCity() ?></li>
                        <li>Ville : <?php echo $accommodation->getAdress()->getCountry() ?></li>
                        <li>Prix : <?php echo $accommodation->getPrice() ?> /nuit</li>
                        <li>Taille : <?php echo $accommodation->getSize() ?> /m²</li>
                        <li>Description : <?php echo $accommodation->getDescription() ?></li>
                        <li>Couchages : <?php echo $accommodation->getBeds() ?></li>
                        <li>
                            <?php if (isset($_SESSION['user'])): ?>
                                <button><a href="/users/create-rental/<?php echo $accommodation->getId() ?>" class="protected-link">Louer</a></button>
                            <?php else: ?>
                                <button><a href="/users/login" class="protected-link">Louer</a></button>
                            <?php endif; ?>
                        </li>
                    </ul>
                </div>
            <?php endforeach ?>
        </div>
    <?php else: ?>
        <div>Aucune location disponible</div>
    <?php endif; ?>
</div>